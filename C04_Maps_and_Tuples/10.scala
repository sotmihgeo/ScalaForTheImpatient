// Type these commands into the REPL, or run scala script.scala

println("Hello".zip("world").toMap)

/*when zipped the two strings results a sequence of tuples containing the characters from the same position in their strings.
A plausible use case could be when you want to create an encoding/decoding character set */